import React from 'react';
import Button from '@mui/material/Button';

const Login = () => {
  return (
    <Button
      href="#text-buttons"
      variant="contained"
    >
      登入
    </Button>
  );
};

export default Login;

